// Premium API placeholder — will be filled after Supabase premium views exist.

import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json({
    error: "Premium endpoints not implemented yet.",
  });
}
